include "locations.conf"

dev {
  base-url: "https://api.openweathermap.org"
  appid: 795046ba9b1e28ff654228de8b76102f
  // In a real world application, the API_KEY would be in a security file; not being able to be check-in (.gitignore); however the file would be leveraged by the CI server to interject it into a docker image.
  // In addition the security file could include usernames, passwords and maybe urls.
  units: imperial
}
stage {
  base-url: ""
  appid: ""
  units: imperial
}
prod {
  base-url: ""
  appid: ""
  units: imperial
}